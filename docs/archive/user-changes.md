on the user profile page, remove the fields for "Platform admin" and "Partners" if they are empty.  Nobody wants to know they're NOT an admin.

I think managers should be able to archive and/or delete Evals.  Archive should get all of its related assets out of view, but keep the data.  We'll also need an archive listing, and a "restore" button.

Wherever the stock JS alert is used, replace it with a nicely styled custom modal popup.
